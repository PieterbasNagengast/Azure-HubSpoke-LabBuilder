name: "LabBuilder - Deploy VWAN Scenarios"

on:
  push:
    branches: none
  pull_request:
    branches: none
  workflow_dispatch:

jobs:
    Deploy:
        uses: ./.github/workflows/deploy.yml
        with:
            LabBuilderType: vwan
            LabBuilderCleanup: true
        secrets:
            inherit
