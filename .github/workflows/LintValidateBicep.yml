name: 'Bicep Test and Validation'

on:
  push:
    branches: none
  pull_request:
    branches: none
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2.4.2

      - name: Bicep Build
        run: | 
          az bicep build --file main.bicep

  validate:
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        scenarios: [A1,A2,A3,B1,B2,B3]

    steps:
      - name: Checkout
        uses: actions/checkout@v2.4.2
        
      - name: hello
        run: |
          echo "Hello world, ${{ matrix.scenarios }}"
      
         
